version: "3"

services:
  postgres:
    image: postgres:15.2
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: delivery
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
